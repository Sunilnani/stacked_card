name: stacked_cards        # lowercase, underscores
description: A Flutter widget that shows animated, tappable stacked cards.
version: 1.1.0
# semantic versioning: MAJOR.MINOR.PATCH

homepage: https://github.com/Sunilnani/stacked_card
repository: https://github.com/Sunilnani/stacked_card
license: MIT

environment:
  sdk: ">=2.18.0 <4.0.0"


dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/screenshot.jpeg
    - assets/demo.gif
    - assets/demo.mp4
